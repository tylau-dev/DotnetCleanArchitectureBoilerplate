# Context
Read .github/AGENT.md (and memories if needed) to understand the current state of the project and carry on with the following task(s).

Make a plan (aka a prompt) that will be run in Agent mode for the following Tasks

# Tasks
Implement application layer with CQRS handlers (Commands/Queries), DTOs, MediatR pipeline behaviors, and minimal XML comments on public contracts only.
